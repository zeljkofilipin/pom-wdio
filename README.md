# pom-wdio
Page object models in WebdriverIO

## How to observe tests
The tests run inside a DevContainer with a virtual desktop. To watch the browser execute in real-time:

1. Open `http://localhost:6080` in your web browser and click **Connect**.
2. In the VS Code terminal, run `npm run wdio`.
3. Watch the browser open on the virtual desktop.